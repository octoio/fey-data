; Include auto-generated rules for .atd files

(include dune.inc)

; Generate the dune.inc file from all .atd files

(rule
 (mode promote)
 (deps
  (glob_files *.atd))
 (action
  (with-stdout-to
   dune.inc
   (run gen/gen.exe))))
